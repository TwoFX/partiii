If $M$ is an $R$-module, then we call a submodule $N$ of $M$ $p$-primary
(or just primary) if $\Ass(M/N) = \set{p}$ for a prime ideal $p$. Since ideals
are just submodules, the definition extends to ideals.
