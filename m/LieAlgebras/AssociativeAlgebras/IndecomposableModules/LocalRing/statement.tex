A ring $R$ is called local if it has a unique maximal right ideal. This ideal
is then necessarily equal to $J(R)$ and it is also the unique maximal left ideal,
because its complement consists of units.
